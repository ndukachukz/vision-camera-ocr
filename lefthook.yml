pre-commit:
  parallel: true
  commands:
    lint:
      glob: "*.{js,ts,jsx,tsx}"
      run: bunx @biomejs/biome check {staged_files}
    types:
      glob: "*.{js,ts, jsx, tsx}"
      run: bunx tsc
commit-msg:
  parallel: true
  commands:
    commitlint:
      run: bunx commitlint --edit
